# LIMINAL — environment variables template
#
# Copy this file to `.env.local` and fill in your own values.
# `.env.local` is gitignored automatically (*.local pattern).
#
# Vite client-side rule: only variables prefixed with `VITE_` are
# exposed to browser code via `import.meta.env`.

# === REQUIRED ===

# QuickNode Solana Mainnet HTTP Provider URL.
# Get yours at: https://dashboard.quicknode.com → Endpoints → [your endpoint]
# Format: https://your-name.solana-mainnet.quiknode.pro/your-token/
VITE_QUICKNODE_RPC_URL=

# === OPTIONAL ===

# Solana cluster. Defaults to "mainnet-beta".
# Use "devnet" for safe flow testing — Kamino + aggregator swaps get
# disabled automatically and a devnet banner is shown.
# VITE_SOLANA_NETWORK=mainnet-beta

# Sentry DSN for production error telemetry. Leave blank to disable
# telemetry entirely — zero data leaves the browser when unset.
# VITE_SENTRY_DSN=

# Aggregator base URL (defaults to Jupiter Ultra lite-api).
# VITE_DFLOW_API_URL=https://lite-api.jup.ag
